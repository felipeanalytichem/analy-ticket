import pg from 'pg'; const client = new pg.Client({ user: 'postgres', host: 'db.plbmgjqitlxedsmdqpld.supabase.co', database: 'postgres', password: process.env.SUPABASE_DB_PASSWORD, port: 5432, ssl: { rejectUnauthorized: false } }); await client.connect(); console.log('Connected to PostgreSQL'); await client.query('DROP TRIGGER IF EXISTS trigger_add_initial_chat_participants ON ticket_chats CASCADE'); console.log('Trigger dropped'); await client.end();
